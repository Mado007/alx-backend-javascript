/**
 * Round the two given numbers and return the sum of them
 * @param {Number} a first number
 * @param {Number} b second number
 */
const calculateNumber = (a, b) => {
  return Math.round(a) + Math.round(b);
};

module.exports = calculateNumber;
